# CLAUDE.md

Claude Code automatically loads this file. The agent instructions for this repo are a single source of truth shared across agents (Claude Code, Codex, Gemini, etc.) and live in [`AGENTS.md`](./AGENTS.md).

Read `AGENTS.md` first. It covers the stack, canonical helpers, error-handling taxonomy, product conventions, architectural notes, layout, editing rules, and **the git workflow** (every change to `main` goes through a PR).
