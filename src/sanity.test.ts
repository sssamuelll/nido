import { describe, it, expect } from 'vitest';

describe('sanity check (delete this PR)', () => {
  it('intentionally fails to verify CI gate blocks merge', () => {
    expect(true).toBe(false);
  });
});
